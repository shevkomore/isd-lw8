import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'isd.lw8',
  appName: 'isd-lw8',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
